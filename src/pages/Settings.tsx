import { backendConfigured,demoMode } from '../lib/supabase'
import { Badge,Panel } from '../components/ui'
export function Settings(){return <div className="space-y-4"><Panel title="Configuração da plataforma" subtitle="Execução independente do Lovable"><div className="grid gap-3 md:grid-cols-2"><Setting title="Backend Supabase/Postgres" value={backendConfigured?'Configurado':'Pendente de variáveis'} tone={backendConfigured?'green':'amber'}/><Setting title="Modo demonstração" value={demoMode?'Ativo':'Desativado'} tone={demoMode?'amber':'green'}/><Setting title="Autenticação" value="Supabase Auth" tone="green"/><Setting title="Segregação SaaS" value="Multi-tenant por organização" tone="green"/></div></Panel><Panel title="Integrações previstas"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{['WhatsApp Business Platform','CRM e discadores','CDR / telefonia','ERP / financeiro','APIs de entrada e saída','Power BI opcional'].map(x=><div className="rounded-2xl bg-white/[.035] p-4 text-xs ring-1 ring-white/7" key={x}>{x}</div>)}</div></Panel><Panel title="Escopo consolidado" subtitle="Duplicidades dos documentos foram unificadas em um único núcleo"><p className="text-xs leading-6 text-white/55">O núcleo concentra dados, planejamento, operação, funis, custos, inteligência, recomendações, decisões, ações, cenários, governança e integrações. CRM completo, discador próprio, WFM completo e speech analytics permanecem como conectores/evoluções, evitando duplicar sistemas que a plataforma deve consumir.</p></Panel></div>}
function Setting({title,value,tone}:{title:string;value:string;tone:'green'|'amber'}){return <div className="flex items-center justify-between rounded-2xl bg-white/[.035] p-4 ring-1 ring-white/7"><span className="text-xs">{title}</span><Badge tone={tone}>{value}</Badge></div>}
